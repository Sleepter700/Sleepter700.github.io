---
# Para actualizar la información de contacto como correo electrónico, número de teléfono, dirección, etc. Por favor, actualice en la tabla `settings.contactInfo` en `src/config/config.toml`

enable: true # Controla la visibilidad de esta sección en todas las páginas donde se utiliza
titleSize: "display-2" # Si el texto del título es muy largo, use un tamaño menor como "display-3", "display-2" o "display-1".
title: "Ayudamos a hacer crecer <br /> su negocio"

image: "/images/banner/startup.png"
description: "Somos una agencia digital que ayuda a las empresas a desarrollar experiencias de usuario inmersivas y atractivas que impulsan el crecimiento al más alto nivel"

button:
  # Consulte el esquema `sharedButton` en `src/sections.schema.ts` para ver todas las opciones de configuración disponibles (ej. enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
  enable: false
  label: "VER NUESTROS TRABAJOS"
  url: "/"
  # hoverEffect: "" # Opcional: text-flip | creative-fill | magnetic | magnetic-text-flip
  # variant: "" # Opcional: fill | outline | text | circle
  # rel: "" # Opcional
  # target: "" # Opcional
---
