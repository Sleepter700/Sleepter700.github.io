---
title: ""
enable: true

options:
  layout: "grid" # masonry | grid | full-width | slider
  appearance: "light" # dark | light - aplicable solo si el layout es "grid"
  limit: 4

button:
  # Consulte el esquema `sharedButton` en `src/sections.schema.ts` para todas las opciones de configuración disponibles (ej: enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
  enable: true
  label: "Ver todo"
  url: "/"
  # hoverEffect: "" # Opcional: text-flip | creative-fill | magnetic | magnetic-text-flip
  # variant: "" # Opcional: fill | outline | text | circle
  # rel: "" # Opcional
  # target: "" # Opcional
---
